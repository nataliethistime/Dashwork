<% content_for(:title, 'Calendar Events') %>
<% content_for(:back_link, root_path) %>
<% content_for(:actions, actions_for(CalendarEvent, :create)) %>

<%= render 'calendar_events/calendar_events', calendar_events: @calendar_events %>

<br>

<%= link_to 'New Calendar Event', new_calendar_event_path, class: 'button is-link' %>
