<%= render IndexTableComponent.new(contacts, [:name, :website, :email, :phone, :tags],
  row_classes: {
    website: 'is-hidden-touch',
    email: 'is-hidden-mobile'
  }
) %>

<% if contacts.respond_to? :total_pages %>
  <%= paginate contacts %>
<% end %>
