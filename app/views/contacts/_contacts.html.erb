<%= render 'common/index_table',
  items: contacts,
  fields: [:name, :website, :email, :phone, :groups],
  row_classes: {
    website: 'is-hidden-mobile',
    email: 'is-hidden-mobile'
  }
%>

<%= paginate contacts %>
