<% content_for(:title, 'Editing Contact') %>
<% content_for(:sidebar) { render 'contacts/sidebar' } %>
<% content_for(:back_link, contact_path(@contact)) %>
<% content_for(:actions, actions_for(@contact, :delete)) %>
<%= render 'form', contact: @contact %>
