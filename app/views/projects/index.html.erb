<% content_for(:title, 'Projects') %>
<% content_for(:back_link, root_path) %>
<% content_for(:actions, actions_for(Project, :create)) %>

<%= render 'projects', projects: @projects %>

<br>

<%= link_to 'New Project', new_project_path, class: 'button is-link' %>
