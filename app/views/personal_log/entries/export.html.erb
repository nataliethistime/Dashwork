<% content_for(:title, 'Export Entries') %>
<% content_for(:back_link, dashboard_home_path) %>

<p class='subtitle'>
  Select the format you want to download:
</p>

<%= link_to 'Markdown', personal_log_export_path(format: :md), class: 'button is-primary is-light' %>

<%= link_to 'Day One', personal_log_export_path(format: :text), class: 'button is-primary is-light' %>
