<% content_for(:title, 'All Forms') %>
<% content_for(:back_link, dashboard_home_path) %>
<% content_for(:actions, actions_for(Form, :create)) %>

<%= render 'forms/forms', forms: @forms %>

<br>

<%= link_to 'New Form', new_form_path, class: 'button is-link' %>
