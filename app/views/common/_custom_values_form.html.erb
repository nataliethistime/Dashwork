<% if !item.new_record? %>
  <% item.build_custom_values.each do |cv| %>
    <%= form.fields_for :custom_values, cv do |cv_form| %>
      <%= cv_form.input :value, label: cv.field.name, as: cv.form_field_type %>
      <%= cv_form.input :field_id, as: :hidden %>
    <% end %>
  <% end %>
<% end %>
