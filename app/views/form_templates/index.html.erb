<% content_for(:title, 'Form Templates') %>
<% content_for(:back_link, root_path) %>
<% content_for(:actions, actions_for(FormTemplate, :create)) %>

<%= render 'form_templates/form_templates', form_templates: @form_templates %>

<br>

<%= link_to 'New Form Template', new_form_template_path, class: 'button is-link' %>
