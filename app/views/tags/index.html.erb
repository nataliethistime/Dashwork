<% content_for(:title, 'Tags') %>
<% content_for(:actions) do %>
  <%= link_to polymorphic_path([:new, @type, :tag]), class: 'navbar-item' do %>
    <%= icon_for :create %>
  <% end %>
<% end %>

<% if @tags.empty? %>
  <div>
    You haven't created any <%= @type_plural %> yet.
  </div>
<% else %>
  <%= render IndexTableComponent.new(@tags, [:name, :created_at]) %>
<% end %>
