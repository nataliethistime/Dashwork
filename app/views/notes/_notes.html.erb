<%= render IndexTableComponent.new(
  notes,
  [
    :updated_at,
    :author,
    :content,
    :links,
    :tags
  ],
) %>

<% if notes.respond_to? :total_pages %>
  <%= paginate notes %>
<% end %>
