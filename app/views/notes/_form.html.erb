<%= simple_form_for(@note) do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.association :contact %>
  <%= f.association :company %>
  <%= f.association :project %>
  <%= f.submit %>
<% end %>
