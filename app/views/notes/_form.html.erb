<%= simple_form_for(note) do |f| %>
  <%= render 'common/form_errors', form: f %>
  <%= f.input :content %>
  <%= f.association :contact %>
  <%= f.association :company %>
  <%= f.association :project %>
  <%= f.association :tags, collection: current_user.note_tags.all %>
  <%= f.button :submit %>
<% end %>
