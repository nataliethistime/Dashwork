<%= render IndexTableComponent.new(
  companies.includes(:tags),
  [:name, :website, :email, :phone, :tags],
  sort_fields: [:name],
  q: @q,
  row_classes: {
    website: 'is-hidden-mobile',
    email: 'is-hidden-mobile',
    tags: 'is-hidden-touch',
  }
) %>

<%= paginate companies %>
