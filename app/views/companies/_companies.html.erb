<%= render(
  'common/index_table',
  items: companies,
  item_path: :company_path,
  fields: [:name, :description, :website, :email, :phone, :groups],
  sort_fields: [:name],
  row_classes: {
    description: 'is-hidden-touch',
    website: 'is-hidden-mobile',
    email: 'is-hidden-mobile',
    groups: 'is-hidden-touch',
  }
) %>

<%= paginate companies %>
