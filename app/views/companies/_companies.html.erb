<%= render(
  'common/index_table',
  items: companies.includes(:tags),
  fields: [:name, :website, :email, :phone, :tags],
  sort_fields: [:name],
  row_classes: {
    website: 'is-hidden-mobile',
    email: 'is-hidden-mobile',
    tags: 'is-hidden-touch',
  }
) %>

<%= paginate companies %>
