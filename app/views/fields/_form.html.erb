<%= simple_form_for field do |f| %>
  <%= render 'common/form_errors', form: f %>
  <%= f.input :name %>
  <%= f.input :type, collection: Field::TYPES.map(&:to_sym) %>
  <%= f.button :submit %>
<% end %>
