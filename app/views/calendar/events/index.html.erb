<% content_for(:title, 'Calendar Events') %>
<% content_for(:back_link, dashboard_home_path) %>
<% content_for(:actions) do %>
  <%= render CreateActionComponent.new path: new_calendar_event_path %>
<% end %>

<%= render 'calendar/events/events', calendar_events: @calendar_events %>

<br>

<%= link_to 'New Calendar Event', new_calendar_event_path, class: 'button is-link' %>
